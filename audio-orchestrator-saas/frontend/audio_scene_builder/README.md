# Audio Scene Builder

React component for semantic scene prompting and layer-level controls.

## Features

- Prompt-driven scene generation (`/api/generate-audio`)
- Layer volume/intensity controls
- Output format selection (`wav`, `mp3`, `ogg`)

## Suggested integration

Import `SceneBuilder` into your app shell and pass `apiBaseUrl`.
